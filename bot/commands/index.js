const ImageCommand = require('./image');
const SlapCommand = require('./slap');
const StandardErrorResponse = require('./error');

module.exports = {
  ImageCommand,
  SlapCommand,
  StandardErrorResponse
};
